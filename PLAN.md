# 📋 추가 기능 구현 계획

## 1. 인크루트 크롤링 오류 수정 (우선순위 높음 🔥)
현재 인크루트 공고가 깨져서 나오고, 링크도 기업 페이지로 잘못 연결되는 문제가 있습니다.

### 원인 분석 및 해결 방안
- **인코딩 문제**: `requests`가 자동으로 인코딩을 잡지 못해 글자가 깨짐 -> 강제로 `response.encoding = 'utf-8'` 설정.
- **링크 오류**: 공고 리스트에서 `<a>` 태그 선택자가 기업 정보 링크와 공고 링크를 혼동함 -> 정확히 `jobdb_info/jobpost.asp`가 포함된 링크만 찾도록 필터링 로직 강화.

---

## 2. '직군' 정보 Notion 저장 (간단함 ✅)
현재 앱에서 사용자가 직군(Category)을 선택하면, 이 정보를 Notion의 `직군` 속성(Select)에 저장해달라는 요청입니다.

### 구현 계획
1.  **`scrape_jobs` 함수 변경**:
    - 지금은 공고 리스트(`List[Dict]`)만 반환하는데, 사용자가 선택한 **카테고리 정보**도 각 공고 항목에 `category` 키로 추가해야 함.
    - 예: `{'title': '...', 'category': '마케팅', ...}`
2.  **`notion_bot.py` 수정**:
    - Notion 페이지 생성 시, `job_data`에 `category`가 있으면 `직군` 속성(Select)에 매핑.
    - **주의**: Notion의 Select 속성은 미리 정의된 값과 일치해야 하거나, API가 자동으로 옵션을 생성해줍니다. (보통 자동 생성됨)

---

## 3. 상세 페이지 크롤링 (급여 정보 가져오기) (선택사항 ⚠️)
현재는 목록 페이지만 훑어보고 있어서 "급여" 같은 상세 정보가 비어있습니다.

### 구현 시나리오
1.  **동작 방식**:
    - 목록에서 20개 공고를 찾음.
    - 각 공고마다 URL을 타고 들어가서(`requests.get`) 상세 페이지 HTML 파싱.
    - "급여" 텍스트 추출 후 저장.
2.  **예상되는 문제점 (Trade-off)**:
    - **속도 저하**: 20개 공고를 긁으려면 HTTP 요청을 20번 더 보내야 합니다. (지금보다 20배 느려짐)
    - **차단 위험**: 짧은 시간에 요청을 많이 보내면 사람인/잡코리아에서 IP를 차단할 수 있습니다.
    - **복잡도**: 사이트마다(사람인, 잡코리아, 인크루트) 상세 페이지 구조가 다 달라서 파싱 코드가 3배로 늘어납니다.

### 💡 추천 방향 (제안)
급여 정보가 엄청 중요한 게 아니라면, **상세 페이지 크롤링은 보류**하는 것을 추천합니다. 
대신 **1번(인크루트 수정)**과 **2번(직군 저장)**에 집중해서 빠르고 안정적인 앱을 먼저 만드는 게 좋습니다.

---

## 🚀 결론: 작업 순서
1.  **인크루트 버그 수정**: 글자 깨짐 & 링크 오류 해결
2.  **직군(Category) 저장 기능 추가**: Notion에 '직군' 컬럼 저장
3.  (보류) 급여 상세 크롤링은 속도 문제로 나중에 필요하면 추가

이대로 진행할까요?
